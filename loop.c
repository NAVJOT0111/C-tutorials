#include <stdio.h>
int main(){
    // for(intialisation; condition; statement that will execute after every loop)
    int i;
    for(i = 5; i ; i--){                     // i = true in case of conditon
        printf("%d \n",i);
    }
}